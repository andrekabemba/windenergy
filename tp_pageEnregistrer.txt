import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import 'package:windenergy/features/presentation/widgets/logo.dart';
import 'package:windenergy/features/presentation/widgets/windenergy_button.dart';
import 'package:windenergy/features/presentation/widgets/windenergy_textfield.dart';
import 'package:windenergy/ui/couleurs.dart';

class EnregistrerPage extends StatelessWidget {
  const EnregistrerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        constraints: BoxConstraints.expand(),
        padding: EdgeInsets.all(20),
        color: couleurFond,
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            Logo(),
            SizedBox(height: 30),
            Text(
              "Email",
              style: GoogleFonts.inter(
                fontWeight: FontWeight.bold,
                fontSize: 14,
                color: couleurPrincipale,
              ),
            ),
            SizedBox(height: 10),
            WindenergyTextField(hintText: "Veuillez entrer votre Email ici"),
            SizedBox(height: 20),
            Text(
              "Nom",
              style: GoogleFonts.inter(
                fontWeight: FontWeight.bold,
                fontSize: 14,
                color: couleurPrincipale,
              ),
            ),
            WindenergyTextField(hintText: "Veuillez entrer votre Nom ici"),
            SizedBox(height: 20),
            Text(
              "Prenom",
              style: GoogleFonts.inter(
                fontWeight: FontWeight.bold,
                fontSize: 14,
                color: couleurPrincipale,
              ),
            ),
            SizedBox(height: 10),
            WindenergyTextField(hintText: "Veuillez entrer votre Prenom ici"),
            SizedBox(height: 20),
            Text(
              "Mot de passe",
              style: GoogleFonts.inter(
                fontWeight: FontWeight.bold,
                fontSize: 14,
                color: couleurPrincipale,
              ),
            ),
            SizedBox(height: 10),
            WindenergyTextField(
              hintText: "Veuillez entrer votre mot de passe",
              obscureText: true,
            ),
            SizedBox(height: 20),
            Text(
              "Confirmer mot de Passe",
              style: GoogleFonts.inter(
                fontWeight: FontWeight.bold,
                fontSize: 14,
                color: couleurPrincipale,
              ),
            ),
            SizedBox(height: 10),
            WindenergyTextField(
              hintText: "Veuillez confirmer votre mot de passe",
              obscureText: true,
            ),
            SizedBox(height: 20),
            WindenergyButton(libelle: "S'inscrire", onPressed: () {}),
            SizedBox(height: 20),
            Text(
              "Vous avez déjà un Compte?",
              textAlign: TextAlign.center,
              style: GoogleFonts.inter(color: couleurPrincipale, fontSize: 14),
            ),
            SizedBox(height: 20),
            WindenergyButton(libelle: "Se Connecter", onPressed: () {}),
          ],
        ),
      ),
    );
  }
}
